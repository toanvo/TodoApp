# TodoApp

This is the ToDo App

# TodoApp Back end

Please run the run.sh under BASH application to launch the application.

```bash
./run.sh
```

if you run on MacOS, please run following the commands

```bash
chmod +x run.sh
./run.sh
```

## Note

"There is a lot of room for improvement regarding security, .NET package vulnerabilities, secret strings and keys, URLs, etc.
